{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the demo",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
